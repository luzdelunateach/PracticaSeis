@typeparam Titem

@foreach (var item in ActorMovie)
{
    @ListActors(item)

}
@code {
    [Parameter]
    public List<Titem> ActorMovie { get; set; }
    [Parameter]
    public RenderFragment<Titem> ListActors { get; set; }
    
    [Parameter]
    public bool Loading { get; set; }
}
